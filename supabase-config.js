// Copia desde `supabase-config.example.js` y reemplaza con tus valores reales.
// Este archivo contiene solo la clave pública (anon). Aun así, no lo subas a repositorios públicos si prefieres mantenerlo privado.

window.SUPABASE_URL = 'https://<your-project>.supabase.co';
window.SUPABASE_ANON_KEY = '<public-anon-key>';
// Copia este archivo a `supabase-config.js` y reemplaza con tus valores reales.
// NO subas `supabase-config.js` a git si contiene claves privadas.

window.SUPABASE_URL = 'https://<your-project>.supabase.co';
window.SUPABASE_ANON_KEY = '<public-anon-key>'; // clave pública (anon)
